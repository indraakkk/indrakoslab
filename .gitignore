node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.dev.vars.example
!.env.example

# nix / direnv
.direnv
result
result-*

# Claude Code local runtime state (PID/session lock, not project config)
.claude/scheduled_tasks.lock
